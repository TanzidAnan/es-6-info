const glass = {
    name: 'glass',
    color: 'golden',
    price: 12,
    isCleaned: true
}

console.log(glass)

const keys =Object.keys(glass);
const values =Object.values(glass);
const entries =Object.entries(glass);

console.log(entries)